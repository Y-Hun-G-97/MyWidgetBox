# 📘 MyWidgetBox 마스터 기능 명세 및 UI/UX 디자인 시스템 (Single Source of Truth)

> **[필독 / 경고]**  
> 본 문서는 **MyWidgetBox 프로젝트의 모든 기능 동작 원리, 데이터 모델, 알고리즘, 제스처, 단축키, UI/UX 디자인 규격 및 인터페이스 규칙을 총망라한 절대 기준 문서(Single Source of Truth)**입니다.  
> **인간 개발자 및 AI 어시스턴트는 향후 코드를 수정하거나 신규 기능을 추가할 때 본 문서의 규칙을 단 하나도 위반해서는 안 되며, 기존 동작을 임의로 누락하거나 스타일을 파괴해서는 안 됩니다.**

---

## 📑 목차
1. [프로젝트 아키텍처 및 모듈 구성](#1-프로젝트-아키텍처-및-모듈-구성)
2. [데이터 및 영속성 모델 (Data Persistence Model)](#2-데이터-및-영속성-모델-data-persistence-model)
3. [전체 기능 명세 (Functional Specifications)](#3-전체-기능-명세-functional-specifications)
   - 3.1 세트(Set) 관리 시스템
   - 3.2 위젯 라이프사이클 및 미디어 재생 시스템
   - 3.3 윈도우 레이어, 모서리 마스크, 마우스 잠금 시스템
   - 3.4 마우스 & 키보드 상호작용 제스처 시스템
   - 3.5 스프레드 배치 시스템 (5대 레이아웃 알고리즘)
   - 3.6 백그라운드 성능 보호 및 배터리 절약 시스템
4. [UI/UX 디자인 시스템 규격 (Visual Design System)](#4-uiux-디자인-시스템-규격-visual-design-system)
   - 4.1 톤앤매너 & 디자인 철학
   - 4.2 디자인 토큰 & 컬러 팔레트 (Color Palette)
   - 4.3 타이포그래피 계층 (Typography Scale)
   - 4.4 벡터 아이콘 시스템 (Vector Icon System)
   - 4.5 컨트롤 및 컴포넌트 표준 스타일
5. [화면별 상세 레이아웃 명세 (Screen Layout Specs)](#5-화면별-상세-레이아웃-명세-screen-layout-specs)
   - 5.1 메인 마스터 컨트롤러 창 (`MasterController`)
   - 5.2 위젯 상세 설정 창 (`SettingsDialog`) - 단일 및 그룹 일괄 모드
   - 5.3 폴더 스프레드 전개 창 (`FolderSpreadDialog`)
   - 5.4 세트 관리 창 (`SetManagerDialog`)
   - 5.5 사용 설명서 창 (`GuideDialog`)
   - 5.6 데스크톱 위젯 자체 (`DesktopWidget`)
6. [개발자 및 AI 불변 규칙 (Inviolable Rules & Checklist)](#6-개발자-및-ai-불변-규칙-inviolable-rules--checklist)

---

## 1. 프로젝트 아키텍처 및 모듈 구성

MyWidgetBox는 Windows 데스크톱 환경을 위한 고성능 멀티미디어 위젯 플랫폼입니다. PyQt6 프레임워크와 하드웨어 레벨 Win32 API를 결합하여 설계되었습니다.

### 1) 핵심 모듈별 책임 및 역할

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              MyWidgetBox.py                                 │
│  • MasterController (메인 윈도우, 아코디언 세트 트리, QSS, 전역 폴링 루틴)      │
│  • DesktopWidget (개별 위젯 본체, 레이어 플래그, 미디어 스택, 페인팅 이벤트)     │
└──────┬────────────────┬────────────────┬─────────────────┬──────────────────┘
       │                │                │                 │
┌──────▼──────┐  ┌──────▼──────┐  ┌──────▼──────┐   ┌──────▼──────┐
│settings_    │  │widget_      │  │master_      │   │master_      │
│dialog.py    │  │runtime.py   │  │operations.py│   │sets.py      │
│•SettingsDlg │  │•마우스드래그│  │•임시그룹제어│   │•세트생성/삭제│
│•SpreadDlg   │  │•리사이즈    │  │•스타트업 큐 │   │•세트전환/복사│
│•AnchorPicker│  │•휠/단축키   │  │•GPU가드동기 │   │•프로필정렬   │
└─────────────┘  └─────────────┘  └─────────────┘   └─────────────┘
       │                │                │                 │
┌──────▼────────────────▼────────────────▼─────────────────▼──────────────────┐
│                             mywidgetbox_core.py                             │
│  • render_vector_icon() : Feather 스타일 벡터 아이콘 렌더러                     │
│  • GpuUsageSampler : Windows PDH 카운터 기반 고속 GPU 모니터링                 │
│  • apply_windows_dark_title_bar() : Windows DWM DwmSetWindowAttribute 다크바  │
│  • ask_dark_confirm() : 다크 테마 커스텀 확인 팝업                              │
│  • get_media_native_size() : ffprobe 및 QImageReader 기반 고속 해상도 추출     │
└─────────────────────────────────────────────────────────────────────────────┘
       │                                                   │
┌──────▼───────────────────────┐            ┌──────────────▼──────────────────┐
│  mywidgetbox_ui_primitives.py │            │        desktop_icons.py         │
│  • DownwardComboBox          │            │  • DesktopIconCloneOverlay      │
│  • TreeBranchLine (├, └ 가이드)│            │  • Windows 셸(Progman) 아이콘   │
│  • ProfileRowWidget (호버행) │            │    가상 복제 및 최하단 Z-Order  │
│  • MarqueeSelectionOverlay   │            │    레이어링 처리                │
│  • ElidedLabel (말줄임 자동) │            └─────────────────────────────────┘
└──────────────────────────────┘
```

---

## 2. 데이터 및 영속성 모델 (Data Persistence Model)

모든 데이터는 Windows 레지스트리 / QSettings(`MyHomeApp` 조직명)에 저장됩니다.

### 1) 마스터 컨트롤러 전역 설정 (`QSettings("MyHomeApp", "MasterSettings")`)
- `current_set_id` (str): 메인 UI에서 현재 선택되어 관리 중인 세트 ID
- `applied_set_id` (str): 바탕화면에 실제로 적용되어 실행 중인 세트 ID
- `set_ids` (list[str]): 세트들의 정렬 순서 목록 (예: `['1', '2', '3']`)
- `sets/{sid}/name` (str): 세트의 사용자 지정 이름 (기본값: `세트{sid}`)
- `sets/{sid}/profiles` (list[str]): 해당 세트에 속한 프로필 ID 목록 (예: `['1', '2']`)
- `gpu_guard_high_pct` (int/float): GPU 가드 일시정지 임계치 (기본값: `90.0`)
- `gpu_guard_low_pct` (int/float): GPU 가드 재개 임계치 (기본값: `70.0`)
- `gpu_guard_panel_expanded` (bool): 상단 GPU 설정 패널의 펼침/접힘 상태
- `pause_on_fullscreen` (bool): 다른 앱 전체화면 실행 시 미디어 자동 일시정지 활성화 여부 (기본값: `True`)
- `startup_shortcut_enabled_v1` (bool): Windows 시작 시 자동 실행(작업 스케줄러) 등록 여부

### 2) 개별 위젯 프로필 설정 (`QSettings("MyHomeApp", f"Profile_{pid}")`)
- `name` (str): 프로필 표시 이름 (기본값: `New 세팅`)
- `x`, `y` (int): 바탕화면 상의 위젯 절대 좌표 (px)
- `w`, `h` (int): 위젯 너비 및 높이 (px, 기본값: `200 x 200`)
- `folder_path` (str): 미디어가 들어있는 폴더 경로
- `folder_item_paths` (list[str]): 특정 선택된 미디어 파일들의 절대 경로 목록
- `interval` (int): 슬라이드쇼 재생 간격 (초 단위, 기본값: `5`)
- `opacity_pct` (int): 위젯 불투명도 (10 ~ 100, 기본값: `100`)
- `bg_color_mode` (int): 배경색 모드 (0: 투명, 1: 검정, 2: 화이트, 3: 다크블루)
- `corner_mode` (int): 모서리 스타일 (0: 라운드 16px, 1: 직각 0px, 2: 타원/원형)
- `layer_mode` (int): 윈도우 레이어 (0: 배경/최하단, 1: 일반/기본, 2: 최상위/항상위)
- `is_locked` (bool): 마우스 클릭 통과 잠금 여부 (True 시 `WindowTransparentForInput`)
- `is_muted` (bool): 비디오 미디어 음소거 여부 (기본값: `True`)
- `growth_anchor` (str): 위젯 크기 확장 기준점 (`top-left`, `center`, `bottom-right` 등 9방향)
- `keep_aspect_ratio` (bool): 위젯 크기 조절 시 원본 종횡비 강제 고정 여부
- `auto_fit_slide_media` (bool): 슬라이드 이미지/영상 전환 시 위젯 크기를 미디어 종횡비에 맞게 자동 리사이즈
- `media_fit_mode` (int): 미디어 채움 모드 (0: 원본비율 레터박스, 1: 꽉채움 크롭)
- `video_transition_mode` (int): 비디오 전환 모드 (0: 컷 즉시전환, 1: 듀얼 크로스페이드)
- `video_decode_mode` (int): 비디오 디코딩 모드 (0: 자동/HW, 1: SW 강제)
- `video_dual_fade_ms` (int): 듀얼 비디오 크로스페이드 시간 (ms, 기본값: `300`)
- `gpu_guard_enabled` (bool): 해당 위젯에 GPU 성능 보호(과부하 시 일시정지) 적용 여부

---

## 3. 전체 기능 명세 (Functional Specifications)

### 3.1 세트(Set) 관리 서브시스템
1. **불변식 (Invariant)**: 시스템에는 **최소 1개 이상의 세트**가 항상 존재해야 합니다.
   - 세트가 1개만 남았을 경우 세트 삭제 버튼(`[🗑]`)은 비활성화(회색 `#55667e`) 처리되며 삭제 시도가 차단됩니다.
2. **세트 적용 (Apply Set)**:
   - 세트 헤더의 `[세트 적용]`을 누르면 현재 실행 중인 다른 세트의 위젯들을 일괄 종료하고, 새 세트에 속한 위젯들을 바탕화면에 전개합니다.
   - `fast_set_switch`: 이전 세트와 새 세트에 공통으로 속한 위젯은 껐다 켜지 않고 유지하여 화면 깜빡임을 방지합니다.
   - 적용 중인 세트에는 녹색 `[적용 중]` 뱃지가 헤더에 표시됩니다.
3. **세트 삭제 및 프로필 처리**:
   - 프로필이 포함된 세트를 삭제할 경우 **QMessageBox 모달 확인 창**을 띄웁니다:
     - **[첫번째 세트로 흡수]**: 프로필들을 다른 세트로 보존 이동하고 세트만 삭제.
     - **[프로필도 함께 삭제]**: 프로필 데이터까지 영구 삭제.
     - **[취소]**: 삭제 작업 취소.
   - 프로필이 비어있는 세트는 `ask_dark_confirm`으로 즉시 삭제 확인을 받습니다.
4. **순서 이동 (Reordering)**:
   - 각 세트 헤더의 `[▲]` / `[▼]` 버튼으로 세트 표시 및 관리 순서를 상하로 이동할 수 있습니다. (첫 번째 세트는 위로 이동 비활성화, 마지막 세트는 아래로 이동 비활성화)

### 3.2 위젯 라이프사이클 및 미디어 재생 서브시스템
1. **지원 포맷**:
   - 정적 이미지: `PNG`, `JPG`, `JPEG`, `BMP`, `WEBP`
   - 애니메이션 이미지: `GIF`, `APNG`
   - 동영상 파일: `MP4`, `MOV`, `MKV`, `WEBM`, `AVI`, `M4V`
2. **GIF 애니메이션 & 루프 감지**:
   - `QMovie`를 통해 재생하며, GIF 파일의 루프 횟수(Loop Count)를 실시간 모니터링하여 지정된 루프가 끝나면 다음 미디어로 자동 전환합니다.
3. **비디오 듀얼 플레이어 크로스페이드 (Dual Video Crossfade)**:
   - 영상 전환 시 검은 화면 깜빡임을 방지하기 위해 2개의 `QMediaPlayer`를 교차 운용합니다.
   - A 플레이어가 끝나는 시점에 B 플레이어가 백그라운드에서 프리로드된 후 페이드인(Fade-in)되면서 부드럽게 오버랩 전환됩니다.
4. **스타트업 큐 (Startup Queue)**:
   - 다수의 위젯이 동시에 켜질 때 시스템 리소스 병목(렉)을 방지하기 위해, 비디오 ➔ GIF ➔ 정적 이미지 순서로 시간 간격(50~300ms)을 두고 순차 점진적으로 기동합니다.

### 3.3 윈도우 레이어, 모서리 마스크, 마우스 잠금 시스템
1. **3단계 윈도우 레이어**:
   - **레이어 0 (배경 / 최하단)**: `WindowStaysOnBottomHint` 적용. 윈도우 바탕화면 아이콘 및 일반 앱들보다 뒤에 위치합니다.
     - `desktop_icons.py`의 아이콘 클론 오버레이와 연동되어 바탕화면 아이콘이 가려지지 않습니다.
   - **레이어 1 (일반 / 기본)**: 표준 윈도우 z-order를 따릅니다.
   - **레이어 2 (최상위 / 항상 위)**: `WindowStaysOnTopHint` 적용. 다른 모든 창 위에 항상 표시됩니다.
2. **모서리 스타일 (Corner Mode)**:
   - 라운드(기본): 반경 16px 원형 클리핑 마스크 적용.
   - 직각: 모서리 라운딩 없이 사각형 그대로 표시.
   - 타원/원형: 가로세로 비율에 맞춰 완전한 타원 또는 원형 마스크 적용.
3. **마우스 잠금 (Click-through)**:
   - 잠금 활성화 시 `WindowTransparentForInput` 플래그가 부여되어 모든 마우스 클릭이 위젯을 통과하여 아래 윈도우나 바탕화면으로 전달됩니다.
   - 마스터 창의 체크박스 또는 글로벌 단축키로 언제든 잠금을 해제할 수 있습니다.

### 3.4 마우스 & 키보드 상호작용 제스처 시스템
1. **마우스 휠 단독 조작**: 위젯 위에서 휠을 올리거나 내리면 즉시 이전/다음 미디어로 수동 전환.
2. **Ctrl + 마우스 휠 (투명도 조절)**:
   - 위젯 위에서 `Ctrl` 키를 누르고 휠 조작 시 **5% 단위**로 위젯 투명도 조절 (10% ~ 100%).
   - 임시 그룹으로 선택된 상태일 경우 그룹 전체 위젯의 투명도가 일괄 변경됩니다.
3. **Alt + 마우스 휠 (레이어 변경)**:
   - 위젯 위에서 `Alt` 키를 누르고 휠을 올리면 **일반(1) ➔ 최상위(2)**, 내리면 **일반(1) ➔ 배경(0)**으로 즉각 변경.
   - **[필수 안전 로직]**: 한국어 키보드(한/영 키 인접) 및 창 비활성 상태 대응을 위해 Qt `e.modifiers()` 외에 반드시 `win32api.GetAsyncKeyState(VK_MENU)` 물리적 하드웨어 키 상태를 더블체크해야 함.
   - **[가로 휠 대응]**: 로지텍 등 일부 마우스 드라이버가 Alt 조합 시 세로 휠을 가로 휠(`angleDelta().x()`)로 변환하므로 `y or x` 델타를 모두 취해야 함.
   - 변경 시 화면 중앙에 `[레이어: 최상위 (항상 위)]` 형태의 **액션 HUD**를 즉시 표시.
4. **마우스 드래그 이동 및 8방향 리사이즈**:
   - 위젯 내부 드래그 시 자유 이동.
   - 테두리 및 4개 모서리(36px 반투명 삼각형 핸들) 드래그 시 부드러운 8방향 리사이즈 지원.
   - `Growth Anchor`(확장 기준점)에 따라 리사이즈 및 자동 맞춤 시 고정 축 유지.
5. **우클릭 컨텍스트 메뉴 (화면 맞춤 & 인접 공간 채우기)**:
   - **화면 크기 맞춤 (`fit_to_screen`)**:
     - `keep_aspect_ratio == True`: 원본 비율을 유지하며 화면 중앙에 최대로 핏(Fit)하게 확장 (토글 시 이전 크기로 복구).
     - `keep_aspect_ratio == False`: 종횡비 무시하고 **화면 전체 영역($S_w \times S_h$)을 100% 꽉 채움**.
   - **인접 빈 공간 밀착 채우기 (`snap_fill_available_space`)**:
     - `keep_aspect_ratio == True`: 인접한 다른 위젯 및 화면 경계선까지의 남은 영역 내에서 원본 비율을 유지하며 최대 확장.
     - `keep_aspect_ratio == False`: 종횡비 무시하고 **남은 가로폭(`avail_w`)과 세로높이(`avail_h`) 전체를 100% 꽉 채움**.
6. **마퀴 드래그 박스 선택 (Marquee Selection)**:
   - 바탕화면 빈 공간을 마우스 드래그하여 점선 사각 박스 안에 들어온 위젯들을 일괄 임시 그룹화.

### 3.5 스프레드 배치 시스템 (5대 핵심 레이아웃)
1. **📸 사진첩 행 정돈 (구글포토 스타일, 가로 라인 밀착)**:
   - 각 짤의 고유 종횡비율을 100% 온전히 보존하며, 가로 행 단위로 줄을 맞춰 양쪽 벽면에 칼같이 밀착 정렬하는 포토 앨범 스타일 (사용자 만족도 최고).
2. **🖥️ 모니터 자동 맞춤 (해상도 기반 열/크기 자동 계산)**:
   - **작업표시줄 자동 숨김(Auto-hide) 실시간 감지**: 작업표시줄 상태에 맞춰 모니터 전체 해상도($S_w \times S_h$) 기반으로 최적의 열과 크기를 완전 자동 계산.
3. **📌 핀터레스트 컬럼 (세로 열 메이슨리)**:
   - 사용자가 직접 지정한 열(Cols)과 너비를 기준으로 미디어들이 물 흐르듯 떨어지는 자연스러운 카드형 메이슨리.
4. **🔲 균일 바둑판 - 크롭 채우기**:
   - 전통적인 NxM 동일 크기 사각형 격자로 꽉 채우기.
5. **🖼️ 균일 바둑판 - 원본 비율 유지 (레터박스)**:
   - 동일한 크기의 사각형 안에 원본 비율을 보존하며 여백을 두는 격자.
- **화면 높이 맞춤 자동 압축 (Auto Scale-down Fit-Inside)**:
  - 이미지가 60개 이상 대량일 때 세로 누적 높이가 화면 높이를 초과하면, 모든 타일을 화면 높이(2560x1440)에 맞게 2D 스케일 다운하여 맨 아래 타일까지 단 1px도 잘리지 않고 화면 전체에 쏙 들어가도록 압축 배치.
- **화면 경계 이탈 방지 안전 클램핑 (Screen Safety Clamping)**:
  - 타일 배치가 화면 우측 또는 바닥 경계를 벗어나는 경우, 전체 배치 바운딩 박스를 계산하여 화면 안쪽으로 자동 시프트하여 1px도 화면 밖으로 잘려나가지 않도록 보장.
- **세트 위젯 무결성 보존**:
  - 폴더 스프레드 전개 시 기존 세트에 있던 다른 위젯들을 절대 강제 삭제하지 않고 온전히 보존하며, 스프레드 위젯들을 세트에 안전하게 추가.
- **대량 위젯 생성 성능 최적화**:
  - 스프레드로 수십 개 위젯을 일괄 기동할 때 `defer_reload=True`로 중간 트리 빌드 폭주를 방지하고, 벡터 아이콘 전역 메모리 캐싱으로 렌더링 오버헤드를 제거.
- **런타임 결함 예방 및 무결성 보호 가드 (v1.8)**:
  - **Alt+클릭 포그라운드 가드**: 포토샵/블렌더/게임 등 외부 프로그램 사용 중 Alt+클릭 시 바탕화면 위젯이 오작동으로 잠금 토글되는 간섭 100% 방지.
  - **F8 인접 공간 밀착 안정화**: 다중 모니터 교차 영역 검출 및 화면 경계 클램핑으로 모니터 밖 픽셀 이탈 방지.
  - **일괄 재배치 미디어 핏 동기화**: 임시 그룹 스프레드 재배치 시 타일이 꽉 차도록 `media_fit_mode` 자동 동기화.
  - **손상 미디어 격리 쿨다운 가드**: 폴더 내 모든 파일이 손상되었을 때 발생하는 무한 실패-리셋 루프 차단 (15초 쿨다운).
  - **시작프로그램 등록 이중화 fallback**: Windows 계정 권한/버전별 작업 스케줄러 등록 실패 시 시작프로그램 폴더(`.lnk`) 자동 fallback 보장.

### 3.6 백그라운드 성능 보호 및 배터리 절약 시스템
1. **GPU 가드 (GPU Guard)**:
   - 1초마다 Windows PDH 카운터로 전역 GPU 사용률을 폴링.
   - 사용률이 `gpu_guard_high_pct`(기본 90%)를 초과하여 연속 N회 지속되면 등록된 위젯들의 재생을 일시정지.
   - 사용률이 `gpu_guard_low_pct`(기본 70%) 이하로 안정화되면 자동 재생 재개.
2. **다른 앱 전체화면 시 미디어 자동 일시정지 (Pause on Fullscreen)**:
   - 사용자가 게임, 전체화면 영상(유튜브 F11, OTT 등), 전체화면 작업 창을 띄워 **바탕화면이 완전히 가려졌을 때** 이를 실시간 감지하여 모든 위젯의 GIF 애니메이션 타이머와 비디오 재생을 즉시 일시정지.
   - 전체화면 앱이 최소화되거나 창 모드로 복귀하면 즉시 원래 재생 상태로 자동 복구.
   - 상단 GPU 설정 패널의 `[✓] 다른 앱 전체화면 시 미디어 일시정지` 토글로 언제든 켜고 끌 수 있음.
3. **Windows 시작프로그램 등록**:
   - 일반 단순 레지스트리가 아닌 Windows 작업 스케줄러(`ScheduledTask`) API를 활용하여 관리자 권한 충돌 없이 로그인 시 자동 실행 보장.

---

## 4. UI/UX 디자인 시스템 규격 (Visual Design System)

### 4.1 톤앤매너 & 디자인 철학
- **사이버네틱 다크 네이비 테마 (Cyber Dark Navy Theme)**
- 깊은 심해 네이비(`#121c2b`, `#162233`)를 베이스로 하며, 세련된 메탈릭 블루(`#528bf8`, `#3b68d4`)를 액센트로 사용합니다.
- 조잡한 색상 남발을 금지하며, 계층적인 채도와 명도를 통해 정보의 중요도를 전달합니다.

### 4.2 디자인 토큰 & 컬러 팔레트 (Color Palette)

| 토큰 분류 | 색상 코드 (HEX) | 용도 및 설명 |
| :--- | :--- | :--- |
| **App Background** | `#162233` | 마스터 창, 다이얼로그 본체 최상위 배경 |
| **Surface Background** | `#121c2b` | 인풋 컨테이너, 세트 바디, 스크롤 영역 바탕 |
| **Card Background** | `#162438` / `#1d2c42` | 세트 아코디언 카드, 섹션 카드 배경 |
| **Card Hover** | `#213550` / `#263d5c` | 카드, 행 호버 시 강조 배경 |
| **Card Border** | `#283a54` / `#2e4466` | 카드 테두리 (1.5px solid) |
| **Primary Accent** | `#3b68d4` (Hover: `#4a77e8`) | 주요 실행 버튼, 세트 적용 버튼, 강조 뱃지 |
| **Focus Border** | `#528bf8` | 포커스된 인풋, 활성 세트 카드 테두리 |
| **Secondary Button** | `#283a54` (Hover: `#33486a`) | 보조 작업 버튼, 일반 다이얼로그 버튼 |
| **Danger Button** | `#8f3741` (Hover: `#a3434e`) | 삭제, 제거 버튼 (글자: `#ffe4e9`, 보더: `#b55460`) |
| **Success Badge** | `#10b981` / `#58c796` | '적용 중', '실행 중' 상태 인디케이터 (글자: `#ffffff`) |
| **Text - Title** | `#ffffff` / `#f5f7ff` | 창 제목, 메인 헤더, 세트 이름 (13~17px, bold) |
| **Text - Body/Label** | `#d6e2f7` / `#e6eefc` | 일반 폼 라벨, 체크박스 텍스트 (11~12px, semibold) |
| **Text - Section** | `#92bbf8` | 섹션 소제목 (`font-size: 12px; font-weight: 700;`) |
| **Text - Caption/Hint**| `#8fa7c7` / `#b8c9e6` | 설명 힌트 라벨, 단위 표시 (`px`, `초`) (10~11px) |
| **Text - Disabled** | `#506580` / `#55667e` | 비활성화된 텍스트 및 비활성 아이콘 |

### 4.3 타이포그래피 계층 (Typography Scale)
- **기본 글꼴**: `'Segoe UI', 'Malgun Gothic', '맑은 고딕', -apple-system, sans-serif`
- **타이틀 계층**:
  - 윈도우 메인 타이틀: `17px / 700 (Bold) / #f5f7ff / letter-spacing: 0.3px`
  - 다이얼로그 타이틀: `17px / 700 (Bold) / #eef4ff`
  - 섹션 헤더 (`_add_section_header`): `12px / 700 (Bold) / #92bbf8 / padding: 6px 2px 2px 2px`
  - 세트/프로필 이름: `13px / 700 (Bold) / #ffffff`
  - 폼 라벨 (Form Label): `12px / 600 (SemiBold) / #d6e2f7`
  - 체크박스 텍스트 (QCheckBox): `11~12px / 600 (SemiBold) / #e4ecfb` (마스터창 11px, 다이얼로그 12px)
  - 힌트/캡션 (Hint Label): `10~11px / 400 (Regular) / #8fa7c7` (행간 1.4)

### 4.4 벡터 아이콘 시스템 (Vector Icon System)
모든 아이콘은 `mywidgetbox_core.py`의 `render_vector_icon(name, color, size)`를 사용합니다.

```python
# 사용 예시
btn.setIcon(render_vector_icon("trash", "#e06c75", 13))
```

| 아이콘 키 | 그래픽 형태 | 표준 크기 | 표준 색상 | 주 사용처 |
| :--- | :--- | :--- | :--- | :--- |
| `"folder"` | 탭 달린 폴더 | 16px | `#528bf8` / `#8da8cb` | 세트 헤더 폴더 표시 |
| `"settings"` | 미니멀 슬라이더 기어 | 13~14px | `#8da8cb` | 설정 창 열기, 세트 관리 |
| `"trash"` | 뚜껑 달린 휴지통 | 13px | `#e06c75` (활성) / `#55667e` (비활성) | 세트 삭제, 위젯 삭제 |
| `"play"` | 오른쪽 삼각형 | 12~14px | `#58c796` | 위젯 개별 실행, 일괄 실행 |
| `"stop"` | 둥근 사각형 | 12~14px | `#e06c75` | 위젯 개별 정지, 일괄 정지 |
| `"plus"` | 정십자(+) | 12~14px | `#9cb5d8` | 새 세트 추가, 위젯 추가 |
| `"chevron_down"` | 아래 꺾쇠(∨) | 12px | `#8da8cb` | 세트 펼치기, 순서 내리기 |
| `"chevron_up"` | 위 꺾쇠(∧) | 12px | `#8da8cb` | 세트 순서 올리기 |
| `"chevron_right"`| 오른쪽 꺾쇠(>) | 12px | `#8da8cb` | 세트 접기 상태 |
| `"grid"` | 4분할 사각 그리드 | 14px | `#8da8cb` | 스프레드 배치 다이얼로그 |
| `"screen"` | 모니터 디스플레이 | 14px | `#8da8cb` | 화면 맞춤 버튼 |
| `"aspect"` | 대각선 종횡비 사각 | 14px | `#8da8cb` | 원본 비율 맞춤 버튼 |

### 4.5 컨트롤 및 컴포넌트 표준 스타일

#### 1) 버튼 (Buttons)
- **대형 주요 버튼 (`QPushButton#primaryBtn`)**:
  - `min-height: 40px; border-radius: 10px; font-size: 13px; font-weight: 600;`
  - 배경 `#406bdc`, 테두리 `#5f84e5`, 글자 `#edf2ff` (호버 시 `#4a74e2`)
- **보조 버튼 (`QPushButton#secondaryBtn`)**:
  - `min-height: 40px; border-radius: 10px; font-size: 13px; font-weight: 600;`
  - 배경 `#283a54`, 테두리 `#3f5678`, 글자 `#e5edf8` (호버 시 `#33486a`)
- **위험 버튼 (`QPushButton#dangerBtn`)**:
  - `min-height: 40px; border-radius: 10px; font-size: 13px; font-weight: 600;`
  - 배경 `#8f3741`, 테두리 `#b55460`, 글자 `#ffe8ec` (호버 시 `#a3434e`)
- **행 액션 미니 버튼 (`QPushButton[kind="rowAction"]`)**:
  - `min-width: 26~28px; min-height: 26~28px; border-radius: 7px; border: none; background: transparent;`
  - 호버 시 배경 `#375074` / `#2b456e`

#### 2) 단위 입력 필드 (`_create_unit_input`)
- `QSpinBox` + 단위 라벨(`px`, `초`) 래퍼 컨테이너:
  - 컨테이너: `min-height: 32px; background-color: #121c2b; border: 1px solid #283a54; border-radius: 8px;`
  - 포커스 시: `border: 1.5px solid #528bf8; background-color: #17283f;`
  - 스핀박스: 버튼 숨김, 폰트 13px bold, 글자 `#ffffff`
  - 단위 라벨 (`QLabel#unitBadgeLabel`): 폰트 11px bold, `#7b96b8`, 우측 여백 6px

#### 3) 체크박스 (QCheckBox) - 절대 규칙
- **[경고] `QCheckBox::indicator` 스타일을 CSS로 임의 오버라이드하지 마십시오.**  
  CSS로 인디케이터 이미지를 생략하고 배경색만 주면 OS 네이티브 V자 체크 표시(✓)가 지워지고 단색 사각형으로 깨집니다.
- **표준 체크박스 CSS 규칙**:
  ```css
  QCheckBox {
      color: #e4ecfb;
      font-size: 11~12px;
      font-weight: 600;
      spacing: 6~7px;
      min-height: 22~24px;
  }
  QCheckBox:hover {
      color: #ffffff;
  }
  ```

---

## 5. 화면별 상세 레이아웃 명세 (Screen Layout Specs)

### 5.1 메인 마스터 컨트롤러 창 (`MasterController`)
```
┌────────────────────────────────────────────────────────┐
│ [앱 타이틀]  MyWidgetBox                  [가이드] [새 세트] │
│ ────────────────────────────────────────────────────── │
│ [📊 GPU / 시스템 성능 보호 패널] (접기/펼치기)            │
│   • GPU 일시정지 슬라이더 [ 90% ]                       │
│   • GPU 재개 슬라이더     [ 70% ]                       │
│   • ──────── (구분선 sep_line) ──────────              │
│   • [✓] 시작프로그램 등록                                │
│     └ 힌트: "현재 작업 스케줄러 등록됨"                    │
│   • [✓] 다른 앱 전체화면 시 미디어 일시정지                │
│     └ 힌트: "전체화면 게임/영상 실행 시 GIF·영상 정지"     │
│ ────────────────────────────────────────────────────── │
│ [아코디언 세트 트리 영역 (ScrollArea)]                  │
│ ┌────────────────────────────────────────────────────┐ │
│ │ [∨] 📁 세트 이름 (N개) [적용 중]   [▲] [▼] [⚙] [🗑]   │ │
│ │ ────────────────────────────────────────────────── │ │
│ │  • [✓ 전체선택]  [선택 실행] [선택 정지] [일괄 설정]  │ │
│ │  • ├ [●] 위젯 1  [미디어명] [200x200]  [▶] [⚙] [🗑]   │ │
│ │  • └ [●] 위젯 2  [미디어명] [400x200]  [▶] [⚙] [🗑]   │ │
│ │  • [+ 새 위젯 추가]                                │ │
│ └────────────────────────────────────────────────────┘ │
│ ────────────────────────────────────────────────────── │
│ [바닥글] 총 N개 위젯 실행 중 | [전체 실행] [전체 정지]      │
└────────────────────────────────────────────────────────┘
```
- **창 크기**: 폭 420px, 높이 720px
- **세트 헤더 버튼 배치 순서**:
  `[토글 버튼] ➔ [폴더 아이콘] ➔ [말줄임 제목] ➔ [개수 뱃지] ➔ [적용 뱃지/버튼] ➔ [▲] ➔ [▼] ➔ [⚙]` (세트 관리는 [⚙]에서 이름 변경, 복사, 삭제 수행)

### 5.2 위젯 상세 설정 창 (`SettingsDialog`)
- **창 크기 & 스크롤**: 폭 580px, 높이 최소 420px부터 세로 리사이즈(Vertical Resizable) 지원. 각 탭은 `QScrollArea`로 감싸여 작은 모니터나 높은 배율에서도 하단 `[저장]` / `[취소]` 버튼이 절대 잘리지 않고 항상 고정 노출됨.
- **탭 1: 📁 미디어 & 재생**:
  - 대상 폴더 경로 선택 (`[찾아보기]`)
  - 슬라이드 미디어 목록 편집 (`[목록 편집]`)
  - 슬라이드 재생 간격 (단위 인풋 `[ 5 초 ]`)
  - 비율 자동 맞춤 체크박스
  - 동영상 설정: 영상 전환 모드, 듀얼 페이드 ms, 영상 디코드(HW/SW)
- **탭 2: 📐 위젯 & 외형**:
  - **그룹 일괄 모드(`bulk_mode=True`)일 때**:
    - 최상단에 **와이드 배너 카드(`QFrame#bulkNoticeCard`, `#19273c`)** 표시:
      - `[✓] 각 위젯의 기존 크기 유지 (일괄 변경 안 함)`
      - 캡션: "그룹 내 위젯들의 고유한 종횡비와 크기를 안전하게 보존합니다."
    - 바로 아래에 **스프레드 재배치 카드(`QFrame#bulkSpreadCard`, `#19273c`)** 제공:
      - `[✓] 선택한 위젯들을 스프레드 방식으로 화면에 재정렬` (기본값: False)
      - 체크 시 배치 맞춤(하이브리드/전체화면/테트리스/바둑판), 가로 열(Cols), 위젯 간격(px), 전개 방향(우하단/좌하단 등) 서브 옵션 활성화
      - `전체 화면 자동 꽉 채움` 선택 시 열 개수 입력창 시각적 잠금 및 안내 배지 노출
      - 저장 시 그룹 내 위젯들을 지정된 스프레드 알고리즘에 맞춰 바탕화면에 일괄 재배치
  - 크기 및 종횡비: 너비 `[ 200 px ]`, 높이 `[ 200 px ]`, 가로세로 비율 고정 체크박스
  - 확장 기준점: `GrowthAnchorPicker` 3x3 그리드
  - 표시 및 레이어: 불투명도 슬라이더(10~100%), 레이어 콤보(배경/일반/최상위), 클릭 잠금 체크박스
  - 테마 스타일: 배경색 콤보, 모서리 콤보
- **탭 3: ⚡ 연동 & 시스템**:
  - 음소거 체크박스, GPU 가드 체크박스
  - 프로그램 연동 실행(프로세스 바인딩) 경로 및 포커스 연동 설정

### 5.3 폴더 스프레드 전개 창 (`FolderSpreadDialog`)
- **창 크기**: 폭 640px
- **행/열 및 크기 입력**: 행(Rows), 열(Cols), 기준 너비(Width), 기준 높이(Height), 간격(Margin)
- **맞춤 전략 콤보박스 (`fit_combo`)**:
  - `전체 화면 자동 꽉 채움` 선택 시:
    - 행/열 및 너비/높이 입력 필드와 설명 라벨 전체가 시각적으로 확실하게 비활성화(다크 점선 테두리 및 흐린 텍스트 스타일)됨.
    - 하단에 "✨ 전체 화면 꽉 채움 모드: 화면 해상도에 맞춰 너비·높이·열 개수가 자동으로 정밀 계산됩니다." 안내 배지가 시각적으로 즉각 표시됨.

---

## 6. 개발자 및 AI 불변 규칙 (Inviolable Rules & Checklist)

향후 코드를 작성하거나 수정할 때 **아래 10가지 규칙은 타협할 수 없는 절대 불변식(Invariants)**입니다:

1. **인디케이터 스타일 파괴 금지**: `QCheckBox::indicator`를 섣불리 오버라이드하여 OS 표준 V자 체크마크를 날려먹지 마십시오.
2. **단일 진실 공급원 준수**: 본 문서에 정의된 컬러 토큰, 폰트 크기, 간격 규칙을 벗어난 날것(Raw) 스타일을 추가하지 마십시오.
3. **모달 팝업 계층 안전성**: `QMessageBox`나 다이얼로그를 띄울 때는 항상 `parent=self` 또는 현재 활성화된 모달 다이얼로그를 부모로 전달하여 팝업이 뒤로 숨는 버그를 원천 차단하십시오.
4. **Win32 하드웨어 키 감지 보장**: Alt/Ctrl 등 수식자 키 휠 제스처는 Qt 이벤트뿐만 아니라 `win32api.GetAsyncKeyState` 실시간 하드웨어 감지를 반드시 병행하십시오.
5. **가로/세로 휠 듀얼 폴백**: 마우스 휠 이벤트는 항상 `e.angleDelta().y() or e.angleDelta().x()`로 양방향 델타를 모두 수용하십시오.
6. **최소 1개 세트 보존**: 시스템에는 최소 1개의 세트가 항상 유지되어야 하며, 세트 관리 창([⚙])에서도 마지막 1개 세트의 삭제는 차단되어야 합니다.
7. **개별 크기 보존 원칙**: 다수의 위젯을 임시 그룹으로 묶어 일괄 설정을 열었을 때, 사용자의 명시적 요청 없이 위젯들의 고유 W/H를 강제로 덮어쓰지 마십시오.
8. **백그라운드 미디어 정지 원칙**: 다른 앱이 전체화면으로 실행 중일 때는 백그라운드에서 불필요하게 GIF/비디오가 재생되지 않도록 자동 일시정지 상태를 유지하십시오.
9. **벡터 아이콘 강제**: UI 아이콘은 임의의 이미지 파일을 쓰지 말고 `render_vector_icon()` 벡터 렌더러를 사용하십시오.
10. **빌드 전 무결성 검증**: 코드를 수정한 후에는 반드시 `python -m py_compile` 문법 검사와 단위 테스트를 실행하고, `build_release.cmd`로 정상 릴리스 빌드까지 마친 후 사용자에게 보고하십시오.
